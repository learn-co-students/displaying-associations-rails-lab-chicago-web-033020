<h1>All artists:</h1>

<ul>
    <% @artists.each do |artist| %>
    <li>
        <p><%= link_to artist.name, artist_path(artist) %> - <%= pluralize(artist.song_count, 'song') %></p>
    </li>
    <% end %>
</ul> 